# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Partner' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  # Pods for Partner
  pod 'Polaris'
  pod 'MUKit/Shared'
  pod 'MUKit/Networking'
  pod 'MUKit/ImageCache'

  target 'PartnerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PartnerUITests' do
    # Pods for testing
  end

end
